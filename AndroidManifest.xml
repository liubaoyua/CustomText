<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="liubaoyua.customtext"
    android:versionCode="3"
    android:versionName="1.3.1" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="20" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" android:theme="@android:style/Theme.Holo.Light">
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposedminversion"
            android:value="2.0*" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/describe" />

        <activity
            android:name="liubaoyua.customtext.Settings"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="liubaoyua.customtext.SetText" >
        </activity>
        <activity
            android:name="liubaoyua.customtext.AppList"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="liubaoyua.customtext.About"
            android:label="@string/menu_about" >
        </activity>
    </application>

</manifest>